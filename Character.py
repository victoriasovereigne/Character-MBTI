from json_extractor import *

class Character:
	def __init__(self, name):
		self.name = name
		self.names_in_book = names_in_book
		self.agent = []
		self.patient = []
		self.poss = []
		self.mod = []

	# def add_dictionary(self, dictionary):
	# 	self.dictionary = dictionary

	# def process(self, type, book_file):
	# 	for n in self.names_in_book:
	# 		ext_char = extract_character(n, book_file)
	# 		ext_prop = extract_property(ext_char, type)

	# 		if type == 'agent':
	# 			self.agent.extend()

		

